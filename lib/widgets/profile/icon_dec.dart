import 'package:flutter/material.dart';

class IconDec extends StatelessWidget {
  const IconDec({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
